diff --git a/.github/pr184-hardening-error.txt b/.github/pr184-hardening-error.txt
deleted file mode 100644
index 9c78dde..0000000
--- a/.github/pr184-hardening-error.txt
+++ /dev/null
@@ -1 +0,0 @@
-app/src/benchmark/java/org/oxycblt/auxio/benchmark/BenchmarkFixtureReceiver.kt: expected exactly one replacement target, found 0: '        seedPlaybackQueue(context, songCount, playableFiles)\n    context\n        .getSharedPreferenc'
diff --git a/.github/workflows/android.yml b/.github/workflows/android.yml
index a6464fb..7cbad2c 100644
--- a/.github/workflows/android.yml
+++ b/.github/workflows/android.yml
@@ -9,6 +9,7 @@ on:
       - ".github/workflows/**"
       - "app/**"
       - "musikr/**"
+      - "startup-benchmark/**"
       - "media"
       - "media/**"
       - "build.gradle"
@@ -25,10 +26,12 @@ on:
   pull_request:
     branches:
       - dev
+      - "cx/**"
     paths:
       - ".github/workflows/**"
       - "app/**"
       - "musikr/**"
+      - "startup-benchmark/**"
       - "media"
       - "media/**"
       - "build.gradle"
@@ -64,7 +67,7 @@ jobs:
           persist-credentials: false
 
       - name: Set up JDK 21
-        uses: actions/setup-java@v5
+        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
         with:
           distribution: temurin
           java-version: "21"
@@ -81,7 +84,7 @@ jobs:
           printf 'sdk.dir=%s\n' "${sdk_dir}" > local.properties
 
       - name: Set up Gradle
-        uses: gradle/actions/setup-gradle@v5
+        uses: gradle/actions/setup-gradle@0723195856401067f7a2779048b490ace7a47d7c
         with:
           cache-read-only: ${{ github.event_name == 'pull_request' }}
 
diff --git a/.github/workflows/lint.yml b/.github/workflows/lint.yml
index 6edca5a..8f09429 100644
--- a/.github/workflows/lint.yml
+++ b/.github/workflows/lint.yml
@@ -10,6 +10,7 @@ on:
       - ".github/dependabot.yml"
       - "app/**"
       - "musikr/**"
+      - "startup-benchmark/**"
       - "media"
       - "media/**"
       - "build.gradle"
@@ -25,11 +26,13 @@ on:
   pull_request:
     branches:
       - dev
+      - "cx/**"
     paths:
       - ".github/workflows/**"
       - ".github/dependabot.yml"
       - "app/**"
       - "musikr/**"
+      - "startup-benchmark/**"
       - "media"
       - "media/**"
       - "build.gradle"
@@ -178,7 +181,7 @@ jobs:
 
       - name: Set up JDK 21
         if: steps.format_scope.outputs.run_spotless == 'true'
-        uses: actions/setup-java@v5
+        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
         with:
           distribution: temurin
           java-version: "21"
@@ -197,7 +200,7 @@ jobs:
 
       - name: Set up Gradle
         if: steps.format_scope.outputs.run_spotless == 'true'
-        uses: gradle/actions/setup-gradle@v5
+        uses: gradle/actions/setup-gradle@0723195856401067f7a2779048b490ace7a47d7c
         with:
           cache-read-only: ${{ github.event_name == 'pull_request' }}
 
@@ -249,7 +252,7 @@ jobs:
           persist-credentials: false
 
       - name: Set up JDK 21
-        uses: actions/setup-java@v5
+        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
         with:
           distribution: temurin
           java-version: "21"
@@ -266,7 +269,7 @@ jobs:
           printf 'sdk.dir=%s\n' "${sdk_dir}" > local.properties
 
       - name: Set up Gradle
-        uses: gradle/actions/setup-gradle@v5
+        uses: gradle/actions/setup-gradle@0723195856401067f7a2779048b490ace7a47d7c
         with:
           cache-read-only: ${{ github.event_name == 'pull_request' }}
 
@@ -313,7 +316,7 @@ jobs:
           persist-credentials: false
 
       - name: Set up JDK 21
-        uses: actions/setup-java@v5
+        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
         with:
           distribution: temurin
           java-version: "21"
@@ -330,7 +333,7 @@ jobs:
           printf 'sdk.dir=%s\n' "${sdk_dir}" > local.properties
 
       - name: Set up Gradle
-        uses: gradle/actions/setup-gradle@v5
+        uses: gradle/actions/setup-gradle@0723195856401067f7a2779048b490ace7a47d7c
         with:
           cache-read-only: ${{ github.event_name == 'pull_request' }}
 
diff --git a/.github/workflows/manual-release.yml b/.github/workflows/manual-release.yml
index ae62690..ac0df44 100644
--- a/.github/workflows/manual-release.yml
+++ b/.github/workflows/manual-release.yml
@@ -60,6 +60,7 @@ jobs:
           fetch-depth: 0
           fetch-tags: true
           submodules: false
+          persist-credentials: false
 
       - name: Verify branch release context
         shell: bash
@@ -108,13 +109,13 @@ jobs:
           } >> "$GITHUB_OUTPUT"
 
       - name: Set up JDK 21
-        uses: actions/setup-java@v5
+        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
         with:
           distribution: temurin
           java-version: "21"
 
       - name: Set up Gradle
-        uses: gradle/actions/setup-gradle@v5
+        uses: gradle/actions/setup-gradle@0723195856401067f7a2779048b490ace7a47d7c
         with:
           cache-cleanup: always
 
@@ -235,7 +236,12 @@ jobs:
           asset_names=()
           while IFS= read -r variant; do
             [[ -z "${variant}" ]] && continue
-            asset_names+=("${names[$variant]}")
+            base_name="${names[$variant]}"
+            asset_names+=(
+              "${base_name}"
+              "${base_name}.sha256"
+              "${base_name}.metadata.txt"
+            )
           done <<< "${SELECTED_VARIANTS}"
           {
             echo "release_tag=${RELEASE_TAG}"
@@ -383,10 +389,15 @@ jobs:
           set -euo pipefail
           BUILD_TOOLS_DIR="$(find "${ANDROID_HOME}"/build-tools -mindepth 1 -maxdepth 1 -type d | sort -V | tail -n1)"
           APKSIGNER="${BUILD_TOOLS_DIR}/apksigner"
+          AAPT="${BUILD_TOOLS_DIR}/aapt"
           if [[ ! -x "${APKSIGNER}" ]]; then
             echo "::error::apksigner was not found under ANDROID_HOME build-tools."
             exit 1
           fi
+          if [[ ! -x "${AAPT}" ]]; then
+            echo "::error::aapt was not found under ANDROID_HOME build-tools."
+            exit 1
+          fi
           find_one_apk() {
             local variant="$1"
             local apk_dir="$2"
@@ -402,6 +413,57 @@ jobs:
             fi
             printf '%s\n' "${apks[0]}"
           }
+          write_release_sidecars() {
+            local published_path=$1
+            local published_name=$2
+            local source_apk=$3
+            local asset_kind=$4
+            local checksum_path="${RUNNER_TEMP}/${published_name}.sha256"
+            local metadata_path="${RUNNER_TEMP}/${published_name}.metadata.txt"
+            local digest
+            local badging
+            local package_id
+            local version_code
+            local version_name
+            local min_sdk
+            local target_sdk
+            local abis
+
+            digest="$(sha256sum "${published_path}" | awk '{print $1}')"
+            printf '%s  %s\n' "${digest}" "${published_name}" > "${checksum_path}"
+
+            badging="$("${AAPT}" dump badging "${source_apk}")"
+            package_id="$(sed -n -E "s/^package: name='([^']+)'.*/\1/p" <<< "${badging}" | head -n1)"
+            version_code="$(sed -n -E "s/^package:.*versionCode='([^']+)'.*/\1/p" <<< "${badging}" | head -n1)"
+            version_name="$(sed -n -E "s/^package:.*versionName='([^']+)'.*/\1/p" <<< "${badging}" | head -n1)"
+            min_sdk="$(sed -n -E "s/^sdkVersion:'([^']+)'.*/\1/p" <<< "${badging}" | head -n1)"
+            target_sdk="$(sed -n -E "s/^targetSdkVersion:'([^']+)'.*/\1/p" <<< "${badging}" | head -n1)"
+            abis="$(unzip -Z1 "${source_apk}" | sed -n -E 's#^lib/([^/]+)/.*#\1#p' | sort -u | paste -sd, -)"
+            [[ -n "${abis}" ]] || abis='none'
+
+            {
+              echo "asset=${published_name}"
+              echo "asset_kind=${asset_kind}"
+              echo "asset_sha256=${digest}"
+              echo "source_commit=$(git rev-parse HEAD)"
+              echo "release_tag=${RELEASE_TAG}"
+              echo "application_id=${package_id}"
+              echo "version_name=${version_name}"
+              echo "version_code=${version_code}"
+              echo "min_sdk=${min_sdk}"
+              echo "target_sdk=${target_sdk}"
+              echo "abis=${abis}"
+              echo
+              echo '[apksigner certificates]'
+              "${APKSIGNER}" verify --verbose --print-certs "${source_apk}"
+            } > "${metadata_path}"
+
+            cp "${checksum_path}" "${artifact_dir}/${published_name}.sha256"
+            cp "${metadata_path}" "${artifact_dir}/${published_name}.metadata.txt"
+            asset_paths+=("${checksum_path}" "${metadata_path}")
+            asset_names+=("${published_name}.sha256" "${published_name}.metadata.txt")
+          }
+
           artifact_dir="${RUNNER_TEMP}/selected-release-assets"
           rm -rf -- "${artifact_dir}"
           mkdir -p -- "${artifact_dir}"
@@ -447,10 +509,22 @@ jobs:
                 exit 1
                 ;;
             esac
+            if [[ -f ./scripts/check-startup-performance-contracts.sh ]]; then
+              bash ./scripts/check-startup-performance-contracts.sh "${apk_path}"
+            else
+              echo "::warning::This existing release tag predates startup-profile validation."
+            fi
+
             artifact_path="${artifact_dir}/${asset_name}"
             cp "${asset_path}" "${artifact_path}"
             asset_paths+=("${asset_path}")
             asset_names+=("${asset_name}")
+            if [[ "${variant}" == topway_twmusic_magisk ]]; then
+              asset_kind='magisk-systemless-overlay'
+            else
+              asset_kind='signed-apk'
+            fi
+            write_release_sidecars "${asset_path}" "${asset_name}" "${apk_path}" "${asset_kind}"
           done <<< "${SELECTED_VARIANTS}"
           {
             printf 'asset_paths<<EOF\n'
@@ -492,6 +566,7 @@ jobs:
           RELEASE_COMMIT_SHA: ${{ steps.version_commit.outputs.release_commit_sha }}
         run: |
           set -euo pipefail
+          gh auth setup-git
           git fetch --tags origin "${GITHUB_REF_NAME}"
           REMOTE_HEAD="$(git rev-parse "origin/${GITHUB_REF_NAME}")"
           RELEASE_PARENT_SHA="$(git rev-parse "${RELEASE_COMMIT_SHA}^")"
@@ -549,6 +624,8 @@ jobs:
                 *standard-release.apk) printf -- "- \`%s\` keeps the normal Auxio-TS package identity (\`org.oxycblt.auxio\`).\n" "${asset}" ;;
                 *topway-twmedia-release.apk) printf -- "- \`%s\` targets DoFun/Topway alternate entry (\`com.tw.media\`) while exposing \`com.tw.music.MusicActivity\`; TS18 validation is still required.\n" "${asset}" ;;
                 *topway-twmusic-magisk.zip) printf -- "- \`%s\` is a Magisk module that systemlessly overlays the signed \`com.tw.music\` APK at \`/system/priv-app/com.tw.music_a41e/com.tw.music_a41e.apk\`; no raw topwayTwMusic APK asset is published.\n" "${asset}" ;;
+                *.sha256) printf -- "- \`%s\` records the SHA-256 digest of its matching install asset.\n" "${asset}" ;;
+                *.metadata.txt) printf -- "- \`%s\` records package/version/SDK/ABI/signing-certificate evidence for its matching install asset.\n" "${asset}" ;;
               esac
             done <<< "${ASSET_NAMES}"
           } > "${NOTES_FILE}"
@@ -612,6 +689,7 @@ jobs:
           RELEASE_COMMIT_SHA: ${{ steps.version_commit.outputs.release_commit_sha }}
         run: |
           set -euo pipefail
+          gh auth setup-git
           if gh release view "${RELEASE_TAG}" >/dev/null 2>&1; then
             echo "Release ${RELEASE_TAG} exists; keeping remote tag."
             exit 0
diff --git a/.github/workflows/pr184-apply-corrected.yml b/.github/workflows/pr184-apply-corrected.yml
deleted file mode 100644
index 2d6f6f9..0000000
--- a/.github/workflows/pr184-apply-corrected.yml
+++ /dev/null
@@ -1,110 +0,0 @@
-name: PR 184 Apply Corrected Audited Patch
-
-on:
-  push:
-    branches:
-      - cx/startup-profiles-benchmarks
-    paths:
-      - ".github/workflows/pr184-apply-corrected.yml"
-
-permissions:
-  contents: write
-
-concurrency:
-  group: pr184-apply-corrected-audited-patch
-  cancel-in-progress: true
-
-jobs:
-  apply:
-    runs-on: ubuntu-latest
-    timeout-minutes: 10
-    steps:
-      - name: Checkout latest branch head
-        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
-        with:
-          ref: cx/startup-profiles-benchmarks
-          fetch-depth: 0
-          persist-credentials: true
-
-      - name: Apply corrected audited patch and publish result
-        shell: bash
-        run: |
-          set +e
-          log_file="${RUNNER_TEMP}/pr184-hardening.log"
-          {
-            audited_base=95ecb314d4fb1c72e9dbd4049fe406768eebf1c4
-            payload_commit=6548a4df9c8fd87e18f0161db3e603383a691de5
-            target_paths=(
-              .github/workflows/android.yml
-              .github/workflows/lint.yml
-              app/src/benchmark/java/org/oxycblt/auxio/benchmark/BenchmarkFixtureReceiver.kt
-              app/src/main/java/org/oxycblt/auxio/music/MusicRepository.kt
-              app/src/main/java/org/oxycblt/auxio/music/MusicSettings.kt
-              app/src/main/java/org/oxycblt/auxio/music/StartupLibraryPolicy.kt
-              app/src/main/java/org/oxycblt/auxio/util/PerfTimer.kt
-              app/src/test/java/org/oxycblt/auxio/music/MusicSettingsIndexingTriggerTest.kt
-              app/src/test/java/org/oxycblt/auxio/music/StartupLibraryPolicyTest.kt
-              docs/architecture/STARTUP_PROFILES_BENCHMARKS.md
-              docs/architecture/database-first-library.md
-              scripts/check-startup-performance-contracts.sh
-            )
-            git checkout "${audited_base}" -- "${target_paths[@]}"
-            source_workflow="${RUNNER_TEMP}/pr184-original-hardening.yml"
-            patch_script="${RUNNER_TEMP}/pr184-final-hardening.py"
-            git show "${payload_commit}:.github/workflows/pr184-final-hardening.yml" > "${source_workflow}"
-            python3 - "${source_workflow}" "${patch_script}" <<'EXTRACT'
-          from pathlib import Path
-          import sys
-
-          source = Path(sys.argv[1]).read_text(encoding="utf-8").splitlines()
-          start = next(index for index, line in enumerate(source) if line.strip() == "python3 - <<'PY'")
-          end = next(index for index in range(start + 1, len(source)) if source[index].strip() == "PY")
-          indent = " " * 10
-          payload = [line[len(indent) :] if line.startswith(indent) else line for line in source[start + 1 : end]]
-          text = "\n".join(payload) + "\n"
-          wrong = "                    priorState = musicSettings.libraryState,\n                lastScanFailed = { musicSettings.lastScanFailed },"
-          correct = "                    priorState = musicSettings.libraryState,\n                    lastScanFailed = { musicSettings.lastScanFailed },"
-          if text.count(wrong) != 1:
-              raise SystemExit(f"expected one known indentation defect, found {text.count(wrong)}")
-          Path(sys.argv[2]).write_text(text.replace(wrong, correct, 1), encoding="utf-8")
-          EXTRACT
-            python3 -m py_compile "${patch_script}"
-            python3 "${patch_script}"
-          } >"${log_file}" 2>&1
-          status=$?
-          set -e
-          cat "${log_file}"
-
-          git config user.name "github-actions[bot]"
-          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
-          git rm -f -- .github/workflows/pr184-apply-corrected.yml 2>/dev/null || true
-
-          if ((status != 0)); then
-            cp "${log_file}" .github/pr184-hardening-error.txt
-            git add -- .github/pr184-hardening-error.txt .github/workflows
-            git commit -m "ci: capture corrected PR 184 hardening failure"
-            git push origin HEAD:cx/startup-profiles-benchmarks
-            exit 0
-          fi
-
-          rm -f -- .github/pr184-hardening-error.txt
-          bash -n scripts/check-startup-performance-contracts.sh
-          python3 scripts/summarize-startup-benchmarks.py --self-test
-          ruby -e 'require "yaml"; ARGV.each { |f| Psych.safe_load(File.read(f), permitted_classes: [], permitted_symbols: [], aliases: false); puts "OK #{f}" }' .github/workflows/*.yml
-          git add -- \
-            .github/workflows/android.yml \
-            .github/workflows/lint.yml \
-            app/src/benchmark/java/org/oxycblt/auxio/benchmark/BenchmarkFixtureReceiver.kt \
-            app/src/main/java/org/oxycblt/auxio/music/MusicRepository.kt \
-            app/src/main/java/org/oxycblt/auxio/music/MusicSettings.kt \
-            app/src/main/java/org/oxycblt/auxio/music/StartupLibraryPolicy.kt \
-            app/src/main/java/org/oxycblt/auxio/util/PerfTimer.kt \
-            app/src/test/java/org/oxycblt/auxio/music/MusicSettingsIndexingTriggerTest.kt \
-            app/src/test/java/org/oxycblt/auxio/music/StartupLibraryPolicyTest.kt \
-            docs/architecture/STARTUP_PROFILES_BENCHMARKS.md \
-            docs/architecture/database-first-library.md \
-            scripts/check-startup-performance-contracts.sh \
-            .github/workflows
-          git diff --cached --check
-          git commit -m "fix: apply corrected startup hardening patch"
-          git push origin HEAD:cx/startup-profiles-benchmarks
diff --git a/.github/workflows/pr184-hardening-v4.yml b/.github/workflows/pr184-hardening-v4.yml
deleted file mode 100644
index 57a4bba..0000000
--- a/.github/workflows/pr184-hardening-v4.yml
+++ /dev/null
@@ -1,509 +0,0 @@
-name: PR 184 Final Hardening V4
-
-on:
-  push:
-    branches:
-      - cx/startup-profiles-benchmarks
-    paths:
