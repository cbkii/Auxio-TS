name: PR 184 Task Discovery Run Identifier

on:
  push:
    branches:
      - automation/pr184-task-discovery
    paths:
      - .github/workflows/pr184-task-run-discovery.yml

permissions:
  actions: read
  contents: write

concurrency:
  group: pr184-task-run-discovery
  cancel-in-progress: true

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout temporary branch
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          ref: automation/pr184-task-discovery
          fetch-depth: 0
          persist-credentials: true

      - name: Record task-discovery workflow run
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          sleep 10
          gh run list \
            --repo "${GITHUB_REPOSITORY}" \
            --branch automation/pr184-task-discovery \
            --workflow "PR 184 Managed Device Task Discovery" \
            --limit 5 \
            --json databaseId,status,conclusion,headSha,createdAt,updatedAt,url \
            > .github/pr184-task-discovery-runs.json
          cat .github/pr184-task-discovery-runs.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/pr184-task-discovery-runs.json
          git commit -m "ci: record task-discovery run identifier"
          git fetch origin automation/pr184-task-discovery
          git rebase origin/automation/pr184-task-discovery
          git push origin HEAD:automation/pr184-task-discovery
