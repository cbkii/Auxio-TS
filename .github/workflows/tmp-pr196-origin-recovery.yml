name: Temporary PR196 origin recovery

on:
  pull_request:
    branches:
      - dev

permissions:
  contents: write

concurrency:
  group: tmp-pr196-origin-recovery
  cancel-in-progress: false

jobs:
  patch:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          ref: fix/first-launch-source-recovery
          fetch-depth: 0
      - name: Apply origin recovery patch
        shell: bash
        run: python3 scripts/tmp-pr196-origin-recovery.py
      - name: Commit origin recovery
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java/org/oxycblt/auxio/home/list/SongListFragment.kt
          git add app/src/main/java/org/oxycblt/auxio/music/MusicRepository.kt
          git add app/src/main/java/org/oxycblt/auxio/music/StartupLibraryPolicy.kt
          git add app/src/main/java/org/oxycblt/auxio/music/service/IndexingHolder.kt
          git commit -m 'fix: coalesce user-visible startup recovery'
          git push origin HEAD:fix/first-launch-source-recovery
