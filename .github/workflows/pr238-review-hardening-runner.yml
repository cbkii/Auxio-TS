name: PR 238 corrected review hardening

on:
  push:
    branches:
      - fix/mediastore-source-authority-stale-rows
    paths:
      - .github/workflows/pr238-review-hardening-runner.yml

permissions:
  contents: write

concurrency:
  group: pr238-corrected-review-hardening
  cancel-in-progress: false

jobs:
  harden:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out exact PR branch
        uses: actions/checkout@v4
        with:
          ref: fix/mediastore-source-authority-stale-rows
          fetch-depth: 0

      - name: Execute guarded hardening patch
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import textwrap

          source = Path('.github/workflows/pr238-review-hardening.yml').read_text()
          marker = "          python3 - <<'PY'\n"
          start = source.index(marker) + len(marker)
          end = source.index("\n          PY", start)
          script = textwrap.dedent(source[start:end])

          # The two descriptor-open blocks intentionally begin with the same pre-fix catch
          # sequence. Allow the guarded replacement helper to consume those duplicate anchors
          # sequentially while still failing closed when an anchor is absent.
          script = script.replace(
              "if count != 1:\n        raise SystemExit(f\"STOP: {path}: expected anchor once, found {count}\")",
              "if count < 1:\n        raise SystemExit(f\"STOP: {path}: required anchor missing\")",
              1,
          )
          Path('/tmp/pr238-review-hardening.py').write_text(script)
          PY
          python3 /tmp/pr238-review-hardening.py

      - name: Prepare repository dependencies
        shell: bash
        run: bash ./scripts/bootstrap-dependencies.sh --profile jvm-tests

      - name: Apply repository Kotlin formatting
        shell: bash
        run: bash ./scripts/ci-gradle.sh spotlessKotlinApply

      - name: Run focused regression tests
        shell: bash
        run: |
          set -euo pipefail
          bash ./scripts/ci-gradle.sh --continue \
            :app:testTopwayTwMediaDebugUnitTest \
            :musikr:testDebugUnitTest

      - name: Remove temporary hardening workflows and validate diff
        shell: bash
        run: |
          set -euo pipefail
          rm -- \
            .github/workflows/pr238-review-hardening.yml \
            .github/workflows/pr238-review-hardening-runner.yml
          git diff --check
          test ! -e .github/workflows/pr238-review-hardening.yml
          test ! -e .github/workflows/pr238-review-hardening-runner.yml

      - name: Commit review hardening
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && { echo 'STOP: review hardening produced no changes' >&2; exit 1; }
          git commit -m "fix: harden MediaStore failure authority and item retention"
          git push origin HEAD:fix/mediastore-source-authority-stale-rows
