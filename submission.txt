Files changed:
- `app/src/main/java/org/oxycblt/auxio/playback/service/SystemPlaybackReceiver.kt` (verified previously)
- `scripts/check-dofun-topway-compat.sh` (verified previously)
- `AGENTS.md`
- `.github/copilot-instructions.md`
- `docs/ts18/launcher-integration/VALIDATION_MATRIX.md`

Resolved Actions:
- **PackageManager caching:** Verified `TopwayLauncherIntegrationCoordinator.kt` caches `isDoFunInstalled` per-process via `lazy`.
- **`chooseAuto` heuristic:** Verified `TopwaySeekPolicyConverter.kt` handles percentages and permilles before clamping to seconds/milliseconds, as covered by existing tests.
- **Formatting:** `spotlessCheck` ran successfully and passed natively without modification.
- **Head-unit CI guardrails:** Modified `scripts/check-dofun-topway-compat.sh` to enforce `TopwayLauncherIntegrationCoordinator` structure rather than relying on obsolete WidgetComponent properties.
- **Documentation:** Appended clear architectural rulings in `AGENTS.md`, `.github/copilot-instructions.md` and `VALIDATION_MATRIX.md`.
- **Wiring check:** Confirmed `TopwayLauncherIntegrationCoordinator` is canonically used in `SystemPlaybackReceiver` and `PlaybackServiceFragment`.
- **Validation:** Executed tests `./gradlew testTopwayTwMusicDebugUnitTest --tests "org.oxycblt.auxio.headunit.topway.*"`, `bash scripts/check-ts18-apk-reference-contracts.sh`, `bash scripts/check-headunit-compat-safety.sh` and `bash scripts/check-native-keep-rules.sh`. They are now completely green.

Note: TS18 hardware validation is still pending. No LSposed/root/platform/system/vendor-service/private-binder work was added.
