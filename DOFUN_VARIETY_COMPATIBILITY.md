# TS18 DoFun launcher compatibility evidence pointer

The canonical current compatibility and architecture document is:

```text
docs/DOFUN_VARIETY_COMPATIBILITY.md
```

Retained exact-device evidence establishes that, while genuine stock `com.tw.music` remained
selected, the fixed DoFun Music surface opened and controlled stock rather than Auxio
`com.tw.media`. It does not establish what DoFun selects when stock is unavailable or not used.

Do not treat this root-level pointer as a second independent authority. Current repository code,
the canonical document, exact APK evidence and present physical runtime evidence control.
